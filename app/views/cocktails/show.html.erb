<%#= @cocktail.name %>
<%# @cocktail.doses.each do |dose| %>
<%#= dose.description %>
<%# end %>



<div class="container">
  <div class="row">
    <h2 class="text-center"><%= @cocktail.name %></h2>
  <ul class="list-unstyled">
    <% @cocktail.doses.each do |dose| %>
    <li><%= dose.description %>
      <i class="fa fa-star"></i>
    <% end %>
    </li>
  </ul>
  </div>
  <p class="text-center"><%= link_to "Precise the Dose", new_cocktail_dose_path(@cocktail) %><p>
</div>
